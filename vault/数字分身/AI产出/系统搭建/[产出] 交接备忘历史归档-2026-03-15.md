---
type: 归档
title: 交接备忘历史归档
created: 2026-03-15
note: 大脑-06 瘦身操作，旧记录从主文件移至此处
tags:
  - 系统/归档
---

# 交接备忘历史归档

> 原 `交接备忘.md` 中大脑-02 ~ 大脑-05 的详细记录，瘦身后归档于此。
> 主文件只保留精简摘要。如需查阅某轮决策细节，来此文件搜索。

---

## 工程-P0 记录（2026-03-15 执行完成）

### 本轮完成事项

| 产出 | 路径 | 状态 |
|------|------|------|
| WF1：对话清洗标注 | `E:\----2\N8N\01-对话清洗标注.json` | ✅ 完成，待导入N8N验证 |
| WF2：用户校对提炼 | `E:\----2\N8N\02-用户校对提炼.json` | ✅ 完成，待导入N8N验证 |
| 测试思路文档 | `E:\----2\N8N\测试思路.md` | ✅ 完成 |

### 工作流关键设计决策

- **WF1过滤规则**：`name.replace(/^\[.*?\]\s*/, '')` 提取核心名后比较，兼容有前缀和无前缀文件
- **字典替换格式**：`~~原文~~==标准实体==`，长别名优先（按长度降序排序）
- **base64兼容**：同时处理 `$json.content`（As Binary OFF）和 `$binary.data.data`（As Binary ON）
- **WF2 originalPath引用**：`读取原始文件` 节点用 `$('过滤待处理').item.json.originalPath` 跨节点引用
- **WF2并行输出**：`对比提炼` → 同时连接 `写入对话存档` 和 `写入信号报告`

---

## 大脑-05 记录（2026-03-15 14:35 → 18:00 结束）

### 指令完成度核对

- P3 画像更新：⏸ 搁置，用户决定先跑通Pipeline，画像更新等向量库有了再做
- 文件结构重组：✅ 架构决策完成，用户逐步确认
- 对话数据库Pipeline：✅ 工程指令已写完
- N8N文件夹：✅ 已创建 `E:\----2\N8N\`

### 本轮核心产出

| 产出 | 路径 |
|------|------|
| 数字真我系统架构 v1.0 | `AI产出/系统搭建/[产出] 数字真我架构决策-2026-03-15.md` |
| **N8N Pipeline工程指令** | `数字分身/待执行指令/[指令] 工程-N8N对话Pipeline.md` |
| **N8N工作流存放文件夹** | `E:\----2\N8N\`（根目录） |

### Pipeline 决策（用户拍板）

| 决策 | 内容 |
|------|------|
| 7步Pipeline | 自动触发→字典替换+标记→格式化→待确认→手动触发→对比提炼→信号报告 |
| 字典替换标记 | `~~原文~~==标准实体==`，用户在OB中删除划线部分即完成校对 |
| 拆为两个工作流 | 01-对话清洗标注（自动30min）+ 02-用户校对提炼（手动触发） |
| 步骤5触发方式 | **A: 手动触发**（用户校对完在N8N点执行） |
| 信号报告 | 写入 `对话存档/信号/[信号] xxx.md`，含接受/拒绝/新增/删除统计 |
| 全部通过GitHub API | 不用本地文件系统，不用AI模型，纯规则处理 |

### 架构决策汇总

| 决策 | 内容 |
|------|------|
| 真我定位 | 真我 = 系统/数据层 = 现实的我 + 数字的我，不是窗口 |
| 三层架构 | 数据层（真我）→ 流程层（顾问/复盘，持久）→ 任务层（大脑N个+执行层，按需） |
| 碎片+上帝 = 基础OS | 所有角色共享，不是顾问独有 |
| 信息流 | 全部通过真我中转，不需要窗口直接通信 |

### 文件结构（用户确认方向，未执行移动）

```
根目录/
├─ N8N\（N8N工作流JSON，已创建）
├─ 真我\（数据层：画像+状态+向量+生态）
├─ 对话导出\  对话存档\  待确认\
├─ 顾问\（持久流程，面向用户）
├─ 复盘\（持久流程，面向审计）
├─ 大脑\（任务层 + 执行层：工程/杂务/架构师）
├─ 通用SK\（碎片+上帝+共享协议）
├─ brain\  .cursor/skills\  CLAUDE.md
```

### N8N Pipeline 调试进展（大脑-05 后半段）

**工作流1 已基本跑通！** replacedCount: 101，字典替换生效。

| 问题 | 修复 | 状态 |
|------|------|------|
| Code节点 `return []` 不合法 | 改为 `return { json: { _skip: true } }` + 加过滤跳过项节点 | ✅ |
| Binary数据读取（N8N v2不把内容放chunk.data里） | 用 `this.helpers.getBinaryDataBuffer($itemIndex, 'data')` | ✅ |
| 字典也是Binary格式读不到 | 加"字典转文本"节点，同样用helpers读取 | ✅ |
| Export Transcript格式 ≠ SpecStory格式 | 改正则：`**User**` / `**Cursor**` | ✅ |
| 写入sha冲突（文件已存在） | 删旧测试文件 | ✅ |
| 文件名双前缀 `[待确认] [导出] xxx` | JSON已修但用户未导入最新版 | ⚠️ 待验证 |

### 大脑-05 教训

| 教训 | 详情 |
|------|------|
| 碎片确认做到了 | 每个架构决策都等用户确认后才往下走 |
| P3画像更新被用户重新排序 | 用户判断：先跑通Pipeline比更新画像更重要 |
| 快速收尾 | 用户说上下文满了，立刻切交付模式，不纠结细节 |
| **又写代码了** | 直接改N8N JSON修了5轮bug，违反大脑操作纪律 |
| 分角色启动 | 用户提出每次开窗口读太多文件浪费token（启动就消耗40%），需要改CLAUDE.md为按角色加载 |

---

## 大脑-04 完整记录（2026-03-15 11:12 → 14:30）

### 指令完成度核对

- P0 确认N8N管道：❌未执行，原因：用户说"不着急"，降级
- P1 架构师窗口执行：✅ 架构师已交付6/6，大脑-04审查+修正完毕
- P2 研究Cursor原生归档：✅ 已决定：Export Transcript + jsonl，SpecStory已卸载删除
- P3 画像更新：❌未执行，原因：优先处理了架构审查、Pipeline设计、SK体系、IDE弹窗排查。延后到大脑-05

### 本轮重要决策和产出

| 决策/产出 | 详情 |
|-----------|------|
| 复盘窗口定位修正 | 从"消化"回归"审计"，架构图提升到与胜率导师并列 |
| SpecStory 卸载 | 已删除文件夹+清理所有核心文档引用 |
| 对话归档方案确定 | Export Transcript 手动导出 → 杂务跑重命名脚本 → 文件进入后续Pipeline |
| 大脑操作纪律 SK | `.cursor/skills/大脑-操作纪律/SKILL.md` |
| 工程操作纪律 SK | `.cursor/skills/工程-操作纪律/SKILL.md` |
| Pipeline 新版设计 | 6层：N8N读+查字典+标注 → 用户OB校对 → N8N信号提取 → 向量库 → ？ → 真我消化 |
| 向量库存储策略 | 基线存一次 + 增量变更摘要，不全量重复存 |
| SK设计约束写入窗口体系 | 关键规则必须写进每个角色专属SK，不能只放通用层（上下文衰减问题） |
| 全局实体字典 | 用户提出需求，待定位原始字典文件，做成SK工具 |

### 大脑-04 教训

| 教训 | 详情 |
|------|------|
| **不要顺手做** | 看到简单任务就想"我来做了算了"——这是最常犯的错。写指令，让工程/杂务做 |
| 大脑-03+04 两轮都犯"写代码" | 说明这是结构性冲动，不是偶发 |
| 脚本注释和输出必须中文 | 用户明确要求 |
| IDE弹窗排查耗时 | Auto-run Ask Every Time 对文件修改不生效，搁置 |

### 架构师交付审查（6件）

| 序号 | 产出 | 评级 |
|------|------|------|
| 1 | 复盘窗口操作手册.md | **已返工修正** |
| 2 | 窗口体系更新 | **已修正** |
| 3 | 窗口自述规则 | 合格 |
| 4 | 00-通用-交接协议 SK | 合格 |
| 5 | 00-通用-窗口自述 SK | 合格 |
| 6 | SK架构框架+_INDEX | 合格 |

---

## 大脑-03 核心产出（2026-03-15 凌晨）

### N8N管道修复——终于跑通

- 工程窗口修了清洗组装节点的binary fallback，但代码里变量引用被吞
- **最终大脑-03自己修了6处变量引用**
- 管道跑通，能产出带正文的[复盘]文件

### SK架构重大升级——窗口=个体=N个SK

- 每个窗口不是一个SK，而是一个**个体**，个体由N个SK组成能力栈
- 通用SK（00-前缀）+ 专属SK（窗口名-前缀）
- SK存放在 `.cursor/skills/`

### 待执行指令机制

- 大脑把任务写入 `待执行指令/[指令] xxx.md`
- 用户开新窗口说"执行指令"，SK自动扫描并执行

### 大脑-03 教训

| 教训 | 详情 |
|------|------|
| 大脑不写代码 | 给工程的指令描述问题和期望结果 |
| 工程连续失败2次→建议换模型 | Sonnet处理不了的复杂度，大脑应主动建议切Opus |
| 窗口自述是强制的 | 每个窗口必须报身份 |

---

## 大脑-02 核心产出（2026-03-14 下午-晚）

### 真我进化架构

- 数字真我升级为独立角色，定位：观察者+理解者
- 真我两个输入源：AI做了什么 + 用户认为什么
- 胜率导师跟真我关联，不跟大脑关联
- 大脑降级为操作调度

### Pipeline 1 五层架构
```
第1层：N8N清洗 → 第2层：用户OB审核 → 第3层：N8N信号提取 → 第4层：真我进化 → 第5层：决策闭环
```

### 大脑-02 教训
- 大脑不给交接文本就不算派了任务
- 给执行层的指令必须具体到路径
- MAX应该给理解你的窗口

---

## 早期关键决策（03-14 早期）

### SK体系
- 外部SK安装前必须走 skill-vetter
- 已安装3个外部SK

### 对话监控废弃
- cursor_capture.py / 对话监控.pyw 正式废弃
- Export Transcript 为唯一对话归档方式

### 审核动作即信号
- 留/删/标错 = 最强画像信号
- AI错误标记 = 幻觉标记
