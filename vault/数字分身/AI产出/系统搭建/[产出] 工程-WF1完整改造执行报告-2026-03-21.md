# 工程：WF1 完整改造执行报告（2026-03-21）

## 窗口

执行-工程（按 `待执行指令/[指令] 工程-WF1完整改造+SPE清理.md`）

## 已完成

### 任务 1：三 SPE 源

- 文件：`N8N/01-A对话清洗标注.json` → 节点 `specstory-preclean`
- 已增加 `speDir3 = path.join(vaultPath, '..', '..', '.specstory', 'history')`（仓库根 `E:\----2\.specstory\history`）
- 已 `collectSpeFrom(speDir3)`

### 任务 2：四目录去重（防增殖）

- 同一节点内：`exportDir`、`pendingDir`、`archiveDir`、`deletedDir` 合并为 `allProcessedFiles`
- `processedSpeNames`：保留无 `[` 前缀文件名；保留原 `[导出]` 正则提取；增加通用尾缀 `(\d{4}-\d{2}-\d{2}_\d{2}-\d{2}(-\d{2})?Z[^\s]*\.md)$`（**含可选秒数**，避免漏匹配）
- 返回值增加 `speTotal`（去重后 SPE 条目总数），便于 n8n 里查看

### 任务 3：Python 输出路径

- 已读 `N8N/scripts/specstory_clean.py`：`main()` **仅** `output_path.write_text`，**不**写 `input_path`
- 已加中文注释标明「绝不回写 SPE 源」

### 任务 4：过滤未处理

- **未改** `list-pending` / `list-archived` / `filter-unprocessed` 代码
- **结论**：对话导出为输入源，不应进「排除表」；排除仍由待确认+对话删除+对话存档 + `过滤未处理` 的 coreName 逻辑承担，与指令描述一致

### 任务 5：根目录副本清理（P1，仅工具，未执行删除）

- 脚本：`N8N/scripts/list_root_spe_duplicate_pairs.py`
- **精确规则**：仅当「同前缀 + 同标题后缀」下**同时存在** `…_HH-MM-SSZ-…` 与 `…_HH-MMZ-…` 时，列出**不带秒数**的文件为删除候选
- **本机一次运行结果**：**25 个**候选（非指令中写的 67；67 可能为粗略计数或旧口径，以本脚本为准）
- **未执行物理删除**——待你在 WF1 端到端测试通过后再删

### 任务 6：端到端测试

- **须在本机 n8n**：重新导入或粘贴更新后的 `01-A对话清洗标注.json` → 手动触发 WF1 → 查看 `SpecStory预清洗` 输出 `cleaned/skipped/errors/speTotal`
- 本报告**未代替**你在浏览器里点执行

## WF3

- 未改 `03-用户校对归档.json`（与指令一致）

## 产出文件清单

| 路径 | 说明 |
|------|------|
| `N8N/01-A对话清洗标注.json` | WF1 改造 |
| `N8N/scripts/specstory_clean.py` | 注释（输出仅到 output_path） |
| `N8N/scripts/list_root_spe_duplicate_pairs.py` | 根 SPE 副本候选列表（只列不删） |
| `N8N/WF1-SPE源与待确认入口说明.md` | 此前已存在，三源说明仍适用 |

## 下一步（用户侧）

1. n8n：**Import** 更新后的 `01-A对话清洗标注.json`（或逐节点粘贴 `SpecStory预清洗` 代码）
2. 启动 n8n 时仍建议 `NODE_FUNCTION_ALLOW_BUILTIN` **含 `child_process`**（指令里示例漏写 `child_process`，WF1 需 `execSync` 调 Python）
3. 测试通过后：若确认删除 25 个候选，可再下指令执行删除或自行删

## 指令状态

- 已将 `待执行指令/[指令] 工程-WF1完整改造+SPE清理.md` 的 `status` 更新为 `done`
